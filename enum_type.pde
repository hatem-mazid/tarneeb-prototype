enum type {
  D, M, H, S;
}

enum cardState {
  initial, humanHand, botHand, onFloor, lastHand, finished;
}

enum position {
  right, top, left, bottom;
}

enum stages {
  distr, assessment, playing, showScore;
}
